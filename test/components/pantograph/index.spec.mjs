import {
  expect
} from 'chai'

import Pantograph from '#engine/components/pantograph'

describe('#engine/components/pantograph', () => {
  it('is an object', () => {
    expect(Pantograph)
      .to.be.an('object')
  })
})
