import {
  expect
} from 'chai'

import Pinion from 'shinkansen-engine/components/pinion'

describe('shinkansen-engine/components/pinion', () => {
  it('is a function', () => {
    expect(Pinion)
      .to.be.a('function')
  })
})
