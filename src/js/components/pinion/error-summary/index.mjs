import debug from 'debug'

const log = debug('shinksansen-engine/components/pinion/error-summary')

log('`shinkansen` is awake')

export { default } from 'shinkansen-pinion/components/sprockets/error-summary'
